name: Solar system workflow

on:
    workflow_dispatch:
    push:
        branches:
            - main
            - 'feature/*'

env:
    MONGO_URI: "mongodb+srv://a199solomon6:Qa4Zc2dKTqaPQDZf@cluster0.nlx0ovj.mongodb.net/"
    MONGO_PASSWORD: "Qa4Zc2dKTqaPQDZf"
    MONGO_USERNAME: "a199solomon6"

jobs:
    unit-testing:
        name: Unit testing
        runs-on: ubuntu-latest
        steps:
        - name: Checkout repository
          uses: actions/checkout@v4

        - name: Setup Nodejs version - 18
          uses: actions/setup-node@v3
          with:
            node-version: 18
        
        - name: Install dependencies
          run: npm install

        - name: Unit testing
          run: npm test

